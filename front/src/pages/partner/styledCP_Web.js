import styled from "styled-components";

const Partner_Wrap = styled.div``;

const Partner_Left = styled.div``;

const Partner_Title = styled.h1``;

const Partner_content = styled.h2``;

const Partner_Right = styled.div``;

const Partner_Right_Section = styled.section``;

const Partner_Card = styled.div``;

const Partner_Card_Logo = styled.img``;

const Partner_Card_Title = styled.h2``;

const Partner_Card_Content = styled.div``;

export {
  Partner_Wrap,
  Partner_Left,
  Partner_Title,
  Partner_content,
  Partner_Right,
  Partner_Right_Section,
  Partner_Card,
  Partner_Card_Logo,
  Partner_Card_Title,
  Partner_Card_Content,
};
