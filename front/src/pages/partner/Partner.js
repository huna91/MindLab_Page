import React from "react";
import {
  Partner_Wrap,
  Partner_Left,
  Partner_Title,
  Partner_content,
  Partner_Right,
  Partner_Right_Section,
  Partner_Card,
  Partner_Card_Logo,
  Partner_Card_Title,
  Partner_Card_Content,
} from "./styledCP_Web";

const Partner = () => {
  return <div>Partner</div>;
};

export default Partner;
